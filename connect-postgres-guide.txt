🔗 Connect Backend to PostgreSQL Database
==========================================

STEP 1: Add DATABASE_URL to Backend Service
- Go to Railway Dashboard
- Click on "JobTrackr" service (your backend, NOT Postgres)
- Click "Variables" tab
- Click "+ Add Variable"

Add this variable:
Name: DATABASE_URL
Value: ${{ Postgres.DATABASE_URL }}

STEP 2: Wait for Auto-Redeploy
- Railway will automatically redeploy your backend
- Wait 2-3 minutes for deployment to complete

STEP 3: Run Database Migrations
After backend redeploys successfully:
- Go to JobTrackr service → Settings → Deploy logs
- Or use Railway CLI: npx prisma migrate deploy

STEP 4: Test API
- Your API should start responding at: https://jobtrackr-production.up.railway.app/health

✅ PostgreSQL is already deployed and ready
✅ Need to add DATABASE_URL variable to backend
✅ Then run migrations to create tables

IMPORTANT: Add DATABASE_URL to "JobTrackr" service, NOT "Postgres" service!
